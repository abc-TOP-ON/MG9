-- ═══════════════════════════════════════════════════════════════
-- 🔥 MG9 ULTIMATE HUB V24 - FULLY FIXED 🔥
-- 👨‍💻 المطور: mo7sma0550
-- ═══════════════════════════════════════════════════════════════

local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local player = Players.LocalPlayer

-- حذف النسخ السابقة
if CoreGui:FindFirstChild("MG9") then CoreGui.MG9:Destroy() end

local gui = Instance.new("ScreenGui", CoreGui)
gui.Name = "MG9"
gui.ResetOnSpawn = false

---------------------------------------
-- متغيرات التحكم
---------------------------------------
local Settings = {
    AntiFling = false,
    AntiLogs = false,
    AntiNV = false,
    AntiClogs = false,
    AntiCmdBar = false,
    AntiAlerts = false,
    AntiKick = false,
    AntiAFK = false,
}

local Spam = {
    Active = false,
    TotalSent = 0,
    InfiniteTime = false,
    InfiniteCopies = false,
    StartTime = os.time(),
}

local currentTheme = "neon"
local customCommands = {}
local lastSafePos = nil

---------------------------------------
-- ✅ متغيرات التحكم في الحلقات (لإيقاف الحمايات فوراً)
---------------------------------------
local ProtectionThreads = {
    AntiFling = nil,
    AntiLogs = nil,
    AntiNV = nil,
    AntiClogs = nil,
    AntiCmdBar = nil,
    AntiAlerts = nil,
    AntiKick = nil,
    AntiAFK = nil,
}

-- متغيرات الإشعارات
local NotificationQueue = {}
local NotificationsActive = true

---------------------------------------
-- ⚡ دالة الضغطة الواحدة
---------------------------------------
local function setupOneClick(button, callback)
    if not button then return end
    button.MouseButton1Click:Connect(callback)
    button.TouchTap:Connect(callback)
    button.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
            callback()
        end
    end)
end

---------------------------------------
-- دوال مساعدة للتصميم
---------------------------------------
local function createCorner(parent, radius)
    local corner = Instance.new("UICorner", parent)
    corner.CornerRadius = UDim.new(0, radius or 10)
    return corner
end

local function createStroke(parent, color, thickness)
    local stroke = Instance.new("UIStroke", parent)
    stroke.Color = color
    stroke.Thickness = thickness or 1.5
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    return stroke
end

---------------------------------------
-- 🔔 نظام الإشعارات الداخلية (المطور: mo7sma0550)
---------------------------------------
local function showNotification(title, message, color, duration)
    duration = duration or 3.5
    
    -- منع الإشعارات المكررة
    if #NotificationQueue > 5 then
        return
    end
    
    local notif = Instance.new("Frame", gui)
    notif.Size = UDim2.new(0, 350, 0, 65)
    notif.Position = UDim2.new(0.5, -175, 0, -80)
    notif.BackgroundColor3 = colors.bg2
    notif.BackgroundTransparency = 0.15
    createCorner(notif, 14)
    createStroke(notif, color or colors.primary, 2)
    notif.ZIndex = 100
    notif.Name = "Notification_" .. os.time()
    
    -- ظل الإشعار
    local shadow = Instance.new("ImageLabel", notif)
    shadow.Size = UDim2.new(1, 10, 1, 10)
    shadow.Position = UDim2.new(0, -5, 0, -5)
    shadow.Image = "rbxassetid://131604529"
    shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
    shadow.ImageTransparency = 0.7
    shadow.ZIndex = 0
    shadow.BackgroundTransparency = 1
    
    -- أيقونة الإشعار
    local iconLabel = Instance.new("TextLabel", notif)
    iconLabel.Size = UDim2.new(0, 35, 1, 0)
    iconLabel.Position = UDim2.new(0, 10, 0, 0)
    iconLabel.Text = title:match("^(..)") or "📌"
    iconLabel.TextColor3 = color or colors.primary
    iconLabel.BackgroundTransparency = 1
    iconLabel.Font = Enum.Font.GothamBold
    iconLabel.TextSize = 22
    iconLabel.TextXAlignment = Enum.TextXAlignment.Center
    
    -- عنوان الإشعار
    local titleLabel = Instance.new("TextLabel", notif)
    titleLabel.Size = UDim2.new(1, -60, 0, 28)
    titleLabel.Position = UDim2.new(0, 50, 0, 5)
    titleLabel.Text = title
    titleLabel.TextColor3 = color or colors.primary
    titleLabel.BackgroundTransparency = 1
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.TextSize = 14
    titleLabel.TextXAlignment = Enum.TextXAlignment.Left
    titleLabel.TextTruncate = Enum.TextTruncate.AtEnd
    
    -- نص الإشعار
    local msgLabel = Instance.new("TextLabel", notif)
    msgLabel.Size = UDim2.new(1, -60, 0, 25)
    msgLabel.Position = UDim2.new(0, 50, 0, 34)
    msgLabel.Text = message
    msgLabel.TextColor3 = colors.text
    msgLabel.BackgroundTransparency = 1
    msgLabel.Font = Enum.Font.Gotham
    msgLabel.TextSize = 11
    msgLabel.TextXAlignment = Enum.TextXAlignment.Left
    msgLabel.TextTruncate = Enum.TextTruncate.AtEnd
    
    -- زر إغلاق الإشعار
    local closeNotif = Instance.new("TextButton", notif)
    closeNotif.Size = UDim2.new(0, 22, 0, 22)
    closeNotif.Position = UDim2.new(1, -28, 0, 5)
    closeNotif.Text = "✕"
    closeNotif.TextColor3 = colors.textDim
    closeNotif.BackgroundTransparency = 1
    closeNotif.Font = Enum.Font.GothamBold
    closeNotif.TextSize = 12
    closeNotif.ZIndex = 101
    
    setupOneClick(closeNotif, function()
        TweenService:Create(notif, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {Position = UDim2.new(0.5, -175, 0, -100)}):Play()
        TweenService:Create(notif, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {BackgroundTransparency = 1}):Play()
        task.wait(0.3)
        notif:Destroy()
    end)
    
    -- إضافة للإشعارات النشطة
    table.insert(NotificationQueue, notif)
    
    -- تحديث مواقع الإشعارات
    local function updateNotificationPositions()
        for i, n in ipairs(NotificationQueue) do
            if n and n.Parent then
                local pos = 15 + (i - 1) * 75
                TweenService:Create(n, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {Position = UDim2.new(0.5, -175, 0, pos)}):Play()
            end
        end
    end
    
    -- تأثير دخول
    notif.Position = UDim2.new(0.5, -175, 0, -100)
    notif.BackgroundTransparency = 1
    TweenService:Create(notif, TweenInfo.new(0.5, Enum.EasingStyle.Quad), {Position = UDim2.new(0.5, -175, 0, 15)}):Play()
    TweenService:Create(notif, TweenInfo.new(0.5, Enum.EasingStyle.Quad), {BackgroundTransparency = 0.15}):Play()
    
    -- تحديث المواقع بعد الدخول
    task.wait(0.2)
    updateNotificationPositions()
    
    -- حذف الإشعار بعد المدة
    task.wait(duration)
    if notif and notif.Parent then
        for i, n in ipairs(NotificationQueue) do
            if n == notif then
                table.remove(NotificationQueue, i)
                break
            end
        end
        
        TweenService:Create(notif, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {Position = UDim2.new(0.5, -175, 0, -100)}):Play()
        TweenService:Create(notif, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {BackgroundTransparency = 1}):Play()
        task.wait(0.3)
        notif:Destroy()
        updateNotificationPositions()
    end
end

-- دالة إشعار سريعة
local function quickNotify(title, message, color)
    showNotification(title, message, color or colors.primary, 2.5)
end

-- دالة إشعار نجاح
local function successNotify(message)
    showNotification("✅ نجاح", message, colors.success, 2.5)
end

-- دالة إشعار خطأ
local function errorNotify(message)
    showNotification("❌ خطأ", message, colors.danger, 3)
end

-- دالة إشعار تحذير
local function warningNotify(message)
    showNotification("⚠️ تحذير", message, colors.warning, 3)
end

-- دالة إشعار معلومات
local function infoNotify(message)
    showNotification("ℹ️ معلومات", message, colors.info, 2.5)
end

---------------------------------------
-- 💾 نظام الحفظ التلقائي
---------------------------------------
local SAVE_KEY = "MG9_Settings_" .. player.UserId

-- حفظ الإعدادات
local function saveSettings()
    pcall(function()
        local data = {
            theme = currentTheme,
            commands = customCommands,
            settings = Settings,
            spamTotal = Spam.TotalSent,
            version = "V24",
            savedAt = os.time(),
        }
        
        local json = HttpService:JSONEncode(data)
        -- حفظ في ذاكرة اللعبة
        if setclipboard then
            setclipboard(json)
        end
        
        -- حفظ في DataStore (إذا كان متاح)
        if player:FindFirstChild("DataStore") then
            player.DataStore.MG9_Settings = json
        end
        
        -- حفظ في اللاعب
        if player:FindFirstChild("MG9_Settings") then
            player.MG9_Settings.Value = json
        else
            local val = Instance.new("StringValue", player)
            val.Name = "MG9_Settings"
            val.Value = json
        end
        
        successNotify("تم حفظ الإعدادات بنجاح")
        print("✅ تم حفظ الإعدادات")
    end)
end

-- تحميل الإعدادات
local function loadSettings()
    pcall(function()
        local json = nil
        
        -- محاولة تحميل من DataStore
        if player:FindFirstChild("DataStore") and player.DataStore:FindFirstChild("MG9_Settings") then
            json = player.DataStore.MG9_Settings.Value
        end
        
        -- محاولة تحميل من اللاعب
        if not json and player:FindFirstChild("MG9_Settings") then
            json = player.MG9_Settings.Value
        end
        
        -- محاولة تحميل من الحافظة
        if not json and getclipboard then
            json = getclipboard()
        end
        
        if json then
            local decoded = HttpService:JSONDecode(json)
            if decoded then
                -- تطبيق الثيم
                if decoded.theme then
                    currentTheme = decoded.theme
                    applyTheme(currentTheme)
                end
                
                -- تطبيق الأوامر المخصصة
                if decoded.commands then
                    customCommands = decoded.commands
                    refreshCommandsList()
                end
                
                -- تطبيق الإعدادات
                if decoded.settings then
                    for key, value in pairs(decoded.settings) do
                        Settings[key] = value
                    end
                end
                
                -- تطبيق عدد الأوامر المرسلة
                if decoded.spamTotal then
                    Spam.TotalSent = decoded.spamTotal
                    sentCount.Text = "📤 " .. Spam.TotalSent
                end
                
                successNotify("تم تحميل الإعدادات بنجاح")
                print("✅ تم تحميل الإعدادات")
                return true
            end
        end
        
        infoNotify("لا توجد إعدادات محفوظة")
        return false
    end)
    return false
end

-- حفظ تلقائي كل 30 ثانية
local function autoSaveLoop()
    task.spawn(function()
        while true do
            task.wait(30)
            if #customCommands > 0 or Spam.TotalSent > 0 then
                saveSettings()
            end
        end
    end)
end

-- حفظ عند إغلاق السكربت
local function saveOnClose()
    saveSettings()
end

---------------------------------------
-- الألوان
---------------------------------------
local function getThemeColors(themeName)
    local themes = {
        neon = {
            primary = Color3.fromRGB(0, 180, 255),
            secondary = Color3.fromRGB(120, 0, 255),
            bg = Color3.fromRGB(10, 10, 25),
            bg2 = Color3.fromRGB(20, 20, 40),
            bg3 = Color3.fromRGB(30, 30, 55),
            text = Color3.fromRGB(255, 255, 255),
            textDim = Color3.fromRGB(170, 170, 190),
            success = Color3.fromRGB(0, 255, 150),
            danger = Color3.fromRGB(255, 60, 60),
            warning = Color3.fromRGB(255, 200, 0),
            gold = Color3.fromRGB(255, 215, 0),
            info = Color3.fromRGB(100, 200, 255),
            accent = Color3.fromRGB(255, 0, 200),
        },
        dark = {
            primary = Color3.fromRGB(100, 100, 255),
            secondary = Color3.fromRGB(150, 50, 255),
            bg = Color3.fromRGB(15, 15, 20),
            bg2 = Color3.fromRGB(25, 25, 35),
            bg3 = Color3.fromRGB(40, 40, 50),
            text = Color3.fromRGB(220, 220, 220),
            textDim = Color3.fromRGB(150, 150, 170),
            success = Color3.fromRGB(100, 255, 100),
            danger = Color3.fromRGB(255, 80, 80),
            warning = Color3.fromRGB(255, 200, 50),
            gold = Color3.fromRGB(200, 180, 100),
            info = Color3.fromRGB(100, 200, 255),
            accent = Color3.fromRGB(200, 100, 255),
        },
        light = {
            primary = Color3.fromRGB(50, 120, 255),
            secondary = Color3.fromRGB(200, 50, 150),
            bg = Color3.fromRGB(240, 240, 245),
            bg2 = Color3.fromRGB(220, 220, 230),
            bg3 = Color3.fromRGB(200, 200, 215),
            text = Color3.fromRGB(30, 30, 40),
            textDim = Color3.fromRGB(80, 80, 100),
            success = Color3.fromRGB(50, 200, 100),
            danger = Color3.fromRGB(220, 50, 50),
            warning = Color3.fromRGB(220, 180, 0),
            gold = Color3.fromRGB(200, 170, 50),
            info = Color3.fromRGB(50, 150, 255),
            accent = Color3.fromRGB(255, 100, 50),
        },
        red = {
            primary = Color3.fromRGB(255, 50, 50),
            secondary = Color3.fromRGB(200, 0, 50),
            bg = Color3.fromRGB(25, 10, 10),
            bg2 = Color3.fromRGB(40, 15, 15),
            bg3 = Color3.fromRGB(55, 25, 25),
            text = Color3.fromRGB(255, 255, 255),
            textDim = Color3.fromRGB(200, 150, 150),
            success = Color3.fromRGB(100, 255, 100),
            danger = Color3.fromRGB(255, 0, 0),
            warning = Color3.fromRGB(255, 200, 0),
            gold = Color3.fromRGB(255, 200, 100),
            info = Color3.fromRGB(255, 150, 150),
            accent = Color3.fromRGB(255, 100, 100),
        },
        green = {
            primary = Color3.fromRGB(0, 255, 100),
            secondary = Color3.fromRGB(0, 200, 150),
            bg = Color3.fromRGB(10, 25, 10),
            bg2 = Color3.fromRGB(15, 40, 20),
            bg3 = Color3.fromRGB(25, 55, 30),
            text = Color3.fromRGB(255, 255, 255),
            textDim = Color3.fromRGB(150, 200, 170),
            success = Color3.fromRGB(50, 255, 150),
            danger = Color3.fromRGB(255, 60, 60),
            warning = Color3.fromRGB(255, 200, 0),
            gold = Color3.fromRGB(200, 255, 100),
            info = Color3.fromRGB(150, 255, 200),
            accent = Color3.fromRGB(100, 255, 150),
        },
        purple = {
            primary = Color3.fromRGB(150, 50, 255),
            secondary = Color3.fromRGB(200, 0, 200),
            bg = Color3.fromRGB(20, 10, 25),
            bg2 = Color3.fromRGB(35, 15, 45),
            bg3 = Color3.fromRGB(50, 25, 60),
            text = Color3.fromRGB(255, 255, 255),
            textDim = Color3.fromRGB(200, 150, 220),
            success = Color3.fromRGB(100, 255, 150),
            danger = Color3.fromRGB(255, 60, 60),
            warning = Color3.fromRGB(255, 200, 0),
            gold = Color3.fromRGB(255, 200, 200),
            info = Color3.fromRGB(200, 150, 255),
            accent = Color3.fromRGB(200, 100, 255),
        },
        ocean = {
            primary = Color3.fromRGB(0, 200, 255),
            secondary = Color3.fromRGB(0, 100, 200),
            bg = Color3.fromRGB(5, 15, 30),
            bg2 = Color3.fromRGB(10, 30, 50),
            bg3 = Color3.fromRGB(20, 45, 70),
            text = Color3.fromRGB(255, 255, 255),
            textDim = Color3.fromRGB(150, 200, 230),
            success = Color3.fromRGB(100, 255, 200),
            danger = Color3.fromRGB(255, 80, 80),
            warning = Color3.fromRGB(255, 220, 50),
            gold = Color3.fromRGB(200, 230, 255),
            info = Color3.fromRGB(150, 220, 255),
            accent = Color3.fromRGB(100, 255, 255),
        },
        sunset = {
            primary = Color3.fromRGB(255, 100, 50),
            secondary = Color3.fromRGB(255, 50, 150),
            bg = Color3.fromRGB(30, 15, 10),
            bg2 = Color3.fromRGB(50, 25, 15),
            bg3 = Color3.fromRGB(70, 40, 25),
            text = Color3.fromRGB(255, 255, 255),
            textDim = Color3.fromRGB(230, 180, 160),
            success = Color3.fromRGB(150, 255, 100),
            danger = Color3.fromRGB(255, 50, 50),
            warning = Color3.fromRGB(255, 200, 50),
            gold = Color3.fromRGB(255, 200, 150),
            info = Color3.fromRGB(255, 180, 150),
            accent = Color3.fromRGB(255, 150, 100),
        },
        cyber = {
            primary = Color3.fromRGB(0, 255, 200),
            secondary = Color3.fromRGB(255, 0, 200),
            bg = Color3.fromRGB(5, 5, 15),
            bg2 = Color3.fromRGB(10, 10, 28),
            bg3 = Color3.fromRGB(20, 15, 40),
            text = Color3.fromRGB(0, 255, 200),
            textDim = Color3.fromRGB(100, 200, 180),
            success = Color3.fromRGB(0, 255, 150),
            danger = Color3.fromRGB(255, 0, 100),
            warning = Color3.fromRGB(255, 200, 0),
            gold = Color3.fromRGB(255, 215, 0),
            info = Color3.fromRGB(0, 255, 220),
            accent = Color3.fromRGB(255, 0, 200),
        },
        royal = {
            primary = Color3.fromRGB(255, 215, 0),
            secondary = Color3.fromRGB(200, 0, 255),
            bg = Color3.fromRGB(15, 8, 30),
            bg2 = Color3.fromRGB(28, 15, 48),
            bg3 = Color3.fromRGB(40, 25, 65),
            text = Color3.fromRGB(255, 215, 0),
            textDim = Color3.fromRGB(200, 180, 150),
            success = Color3.fromRGB(100, 255, 100),
            danger = Color3.fromRGB(255, 50, 50),
            warning = Color3.fromRGB(255, 200, 50),
            gold = Color3.fromRGB(255, 215, 0),
            info = Color3.fromRGB(255, 220, 180),
            accent = Color3.fromRGB(200, 100, 255),
        },
    }
    return themes[themeName] or themes.neon
end

local function applyTheme(themeName)
    currentTheme = themeName
    local colors = getThemeColors(themeName)
    
    mainFrame.BackgroundColor3 = colors.bg
    if mainFrame:FindFirstChild("UIStroke") then
        mainFrame.UIStroke.Color = colors.primary
    end
    
    header.BackgroundColor3 = colors.bg2
    title.TextColor3 = colors.primary
    statusLabel.TextColor3 = colors.textDim
    settingsBtn.TextColor3 = colors.gold
    closeBtn.TextColor3 = colors.danger
    themeBtn.TextColor3 = colors.primary
    infoBtn.TextColor3 = colors.info
    toggleBtn.TextColor3 = colors.text
    
    inputBox.BackgroundColor3 = colors.bg2
    inputBox.TextColor3 = colors.text
    if inputBox:FindFirstChild("UIStroke") then
        inputBox.UIStroke.Color = colors.textDim
    end
    
    settingsFrame.BackgroundColor3 = colors.bg2
    if settingsFrame:FindFirstChild("UIStroke") then
        settingsFrame.UIStroke.Color = colors.gold
    end
    
    infoFrame.BackgroundColor3 = colors.bg2
    if infoFrame:FindFirstChild("UIStroke") then
        infoFrame.UIStroke.Color = colors.info
    end
    
    statusBar.BackgroundColor3 = colors.bg2
    
    for _, child in pairs(scrollContainer:GetChildren()) do
        if child:IsA("Frame") then
            child.BackgroundColor3 = colors.bg2
        end
    end
    
    for _, child in pairs(infoScroll:GetChildren()) do
        if child:IsA("Frame") then
            child.BackgroundColor3 = colors.bg2
        end
    end
    
    statusLabel.Text = "🎨 تم تغيير الثيم"
    statusLabel.TextColor3 = colors.gold
    task.wait(1.5)
    statusLabel.Text = "جاهز للتشغيل"
    statusLabel.TextColor3 = colors.textDim
end

local colors = getThemeColors(currentTheme)

---------------------------------------
-- ✅ أيقونة الدخول/الخروج (مربعة احترافية مع ظل)
---------------------------------------
local toggleBtn = Instance.new("ImageButton", gui)
toggleBtn.Size = UDim2.new(0, 60, 0, 60)
toggleBtn.Position = UDim2.new(1, -75, 0.5, -30)
toggleBtn.BackgroundColor3 = colors.bg2
toggleBtn.Image = "rbxassetid://131604529"
toggleBtn.ImageColor3 = colors.primary
toggleBtn.ImageTransparency = 0.15
toggleBtn.Visible = true
toggleBtn.Draggable = true
createCorner(toggleBtn, 12)
createStroke(toggleBtn, colors.primary, 2.5)

-- ظل احترافي
local shadow = Instance.new("ImageLabel", toggleBtn)
shadow.Size = UDim2.new(1, 8, 1, 8)
shadow.Position = UDim2.new(0, -4, 0, -4)
shadow.Image = "rbxassetid://131604529"
shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
shadow.ImageTransparency = 0.6
shadow.ZIndex = 0
shadow.BackgroundTransparency = 1

local toggleText = Instance.new("TextLabel", toggleBtn)
toggleText.Size = UDim2.new(1, 0, 1, 0)
toggleText.Text = "MG9"
toggleText.TextColor3 = colors.text
toggleText.BackgroundTransparency = 1
toggleText.Font = Enum.Font.GothamBold
toggleText.TextSize = 16
toggleText.ZIndex = 2

-- سحب الأيقونة
local function setupDragging(button)
    local dragging = false
    local dragStart = nil
    local startPos = nil
    
    button.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = button.Position
            button.ZIndex = 100
        end
    end)
    
    button.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            local newPos = UDim2.new(
                startPos.X.Scale + delta.X / (button.Parent.AbsoluteSize.X or 1),
                startPos.X.Offset + delta.X,
                startPos.Y.Scale + delta.Y / (button.Parent.AbsoluteSize.Y or 1),
                startPos.Y.Offset + delta.Y
            )
            newPos = UDim2.new(
                math.clamp(newPos.X.Scale, 0, 1 - (button.Size.X.Scale)),
                newPos.X.Offset,
                math.clamp(newPos.Y.Scale, 0, 1 - (button.Size.Y.Scale)),
                newPos.Y.Offset
            )
            button.Position = newPos
        end
    end)
    
    button.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
            button.ZIndex = 1
        end
    end)
end

setupDragging(toggleBtn)

---------------------------------------
-- الواجهة الرئيسية
---------------------------------------
local mainFrame = Instance.new("Frame", gui)
mainFrame.Size = UDim2.new(0, 380, 0, 520)
mainFrame.Position = UDim2.new(0.5, -190, 0.5, -260)
mainFrame.BackgroundColor3 = colors.bg
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Visible = false
createCorner(mainFrame, 16)
createStroke(mainFrame, colors.primary, 1.5)

---------------------------------------
-- الهيدر
---------------------------------------
local header = Instance.new("Frame", mainFrame)
header.Size = UDim2.new(1, 0, 0, 55)
header.BackgroundColor3 = colors.bg2
header.BackgroundTransparency = 0.3
createCorner(header, 16)

local title = Instance.new("TextLabel", header)
title.Size = UDim2.new(0.35, 0, 1, 0)
title.Position = UDim2.new(0, 15, 0, 0)
title.Text = "✦ MG9"
title.TextColor3 = colors.primary
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextSize = 20
title.TextXAlignment = Enum.TextXAlignment.Left

-- أزرار الهيدر
local infoBtn = Instance.new("TextButton", header)
infoBtn.Size = UDim2.new(0, 28, 0, 28)
infoBtn.Position = UDim2.new(1, -185, 0.5, -14)
infoBtn.Text = "ℹ️"
infoBtn.TextColor3 = colors.info
infoBtn.BackgroundTransparency = 1
infoBtn.TextSize = 16
infoBtn.Font = Enum.Font.GothamBold
infoBtn.ZIndex = 10

local themeBtn = Instance.new("TextButton", header)
themeBtn.Size = UDim2.new(0, 28, 0, 28)
themeBtn.Position = UDim2.new(1, -152, 0.5, -14)
themeBtn.Text = "🎨"
themeBtn.TextColor3 = colors.primary
themeBtn.BackgroundTransparency = 1
themeBtn.TextSize = 16
themeBtn.Font = Enum.Font.GothamBold
themeBtn.ZIndex = 10

local settingsBtn = Instance.new("TextButton", header)
settingsBtn.Size = UDim2.new(0, 28, 0, 28)
settingsBtn.Position = UDim2.new(1, -119, 0.5, -14)
settingsBtn.Text = "⚙️"
settingsBtn.TextColor3 = colors.gold
settingsBtn.BackgroundTransparency = 1
settingsBtn.TextSize = 16
settingsBtn.Font = Enum.Font.GothamBold
settingsBtn.ZIndex = 10

local closeBtn = Instance.new("TextButton", header)
closeBtn.Size = UDim2.new(0, 28, 0, 28)
closeBtn.Position = UDim2.new(1, -86, 0.5, -14)
closeBtn.Text = "🚪"
closeBtn.TextColor3 = colors.danger
closeBtn.BackgroundTransparency = 1
closeBtn.TextSize = 16
closeBtn.Font = Enum.Font.GothamBold
closeBtn.ZIndex = 10

local minimizeBtn = Instance.new("TextButton", header)
minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
minimizeBtn.Position = UDim2.new(1, -50, 0.5, -14)
minimizeBtn.Text = "✕"
minimizeBtn.TextColor3 = colors.textDim
minimizeBtn.BackgroundTransparency = 1
minimizeBtn.TextSize = 16
minimizeBtn.Font = Enum.Font.GothamBold
minimizeBtn.ZIndex = 10

---------------------------------------
-- شريط الحالة
---------------------------------------
local statusBar = Instance.new("Frame", mainFrame)
statusBar.Size = UDim2.new(1, -30, 0, 25)
statusBar.Position = UDim2.new(0, 15, 0, 65)
statusBar.BackgroundColor3 = colors.bg2
createCorner(statusBar, 8)
createStroke(statusBar, colors.textDim, 0.3)

local statusIcon = Instance.new("TextLabel", statusBar)
statusIcon.Size = UDim2.new(0, 25, 1, 0)
statusIcon.Text = "🟢"
statusIcon.TextColor3 = colors.success
statusIcon.BackgroundTransparency = 1
statusIcon.TextSize = 12

local statusLabel = Instance.new("TextLabel", statusBar)
statusLabel.Size = UDim2.new(1, -35, 1, 0)
statusLabel.Position = UDim2.new(0, 30, 0, 0)
statusLabel.Text = "جاهز للتشغيل"
statusLabel.TextColor3 = colors.textDim
statusLabel.BackgroundTransparency = 1
statusLabel.Font = Enum.Font.Gotham
statusLabel.TextSize = 11
statusLabel.TextXAlignment = Enum.TextXAlignment.Left

---------------------------------------
-- علامات تبويب
---------------------------------------
local tabFrame = Instance.new("Frame", mainFrame)
tabFrame.Size = UDim2.new(1, -30, 0, 30)
tabFrame.Position = UDim2.new(0, 15, 0, 98)
tabFrame.BackgroundTransparency = 1

local function createTab(name, pos, callback)
    local btn = Instance.new("TextButton", tabFrame)
    btn.Size = UDim2.new(0.3, -5, 1, 0)
    btn.Position = UDim2.new(pos, 0, 0, 0)
    btn.BackgroundColor3 = colors.bg2
    btn.Text = name
    btn.TextColor3 = colors.textDim
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 11
    createCorner(btn, 6)
    local stroke = createStroke(btn, colors.textDim, 0.5)
    
    setupOneClick(btn, function()
        callback()
        for _, child in pairs(tabFrame:GetChildren()) do
            if child:IsA("TextButton") then
                child.BackgroundColor3 = colors.bg2
                child.TextColor3 = colors.textDim
                if child:FindFirstChild("UIStroke") then
                    child.UIStroke.Color = colors.textDim
                end
            end
        end
        btn.BackgroundColor3 = colors.primary
        btn.BackgroundTransparency = 0.2
        btn.TextColor3 = colors.text
        if btn:FindFirstChild("UIStroke") then
            btn.UIStroke.Color = colors.primary
        end
    end)
    
    return btn
end

local spamTab = createTab("📢 SPAM", 0, function()
    spamContainer.Visible = true
    commandsContainer.Visible = false
    settingsFrame.Visible = false
    infoFrame.Visible = false
end)

local cmdsTab = createTab("📝 أوامر", 0.35, function()
    spamContainer.Visible = false
    commandsContainer.Visible = true
    settingsFrame.Visible = false
    infoFrame.Visible = false
end)

local settingsTab = createTab("⚙️ إعدادات", 0.70, function()
    spamContainer.Visible = false
    commandsContainer.Visible = false
    settingsFrame.Visible = true
    infoFrame.Visible = false
end)

spamTab.BackgroundColor3 = colors.primary
spamTab.BackgroundTransparency = 0.2
spamTab.TextColor3 = colors.text
spamTab.UIStroke.Color = colors.primary

---------------------------------------
-- حاوية SPAM
---------------------------------------
local spamContainer = Instance.new("Frame", mainFrame)
spamContainer.Size = UDim2.new(1, -30, 1, -145)
spamContainer.Position = UDim2.new(0, 15, 0, 135)
spamContainer.BackgroundTransparency = 1
spamContainer.Visible = true

local inputBox = Instance.new("TextBox", spamContainer)
inputBox.Size = UDim2.new(1, 0, 0, 65)
inputBox.Position = UDim2.new(0, 0, 0, 0)
inputBox.PlaceholderText = "📝 اكتب الأوامر... (افصل بـ ;)"
inputBox.Text = ""
inputBox.TextColor3 = colors.text
inputBox.TextSize = 12
inputBox.BackgroundColor3 = colors.bg2
inputBox.MultiLine = true
inputBox.ClearTextOnFocus = false
inputBox.Font = Enum.Font.Gotham
createCorner(inputBox, 8)
createStroke(inputBox, colors.textDim, 0.5)

local settingsRow = Instance.new("Frame", spamContainer)
settingsRow.Size = UDim2.new(1, 0, 0, 28)
settingsRow.Position = UDim2.new(0, 0, 0, 73)
settingsRow.BackgroundTransparency = 1

local timeLabel = Instance.new("TextLabel", settingsRow)
timeLabel.Size = UDim2.new(0.12, 0, 1, 0)
timeLabel.Text = "⏱️"
timeLabel.TextColor3 = colors.textDim
timeLabel.BackgroundTransparency = 1
timeLabel.TextSize = 12
timeLabel.TextXAlignment = Enum.TextXAlignment.Right

local timeBox = Instance.new("TextBox", settingsRow)
timeBox.Size = UDim2.new(0.1, 0, 1, 0)
timeBox.Position = UDim2.new(0.14, 0, 0, 0)
timeBox.Text = "10"
timeBox.TextColor3 = colors.text
timeBox.BackgroundColor3 = colors.bg2
timeBox.TextSize = 11
timeBox.Font = Enum.Font.GothamBold
createCorner(timeBox, 5)
createStroke(timeBox, colors.textDim, 0.3)

local infTimeBtn = Instance.new("TextButton", settingsRow)
infTimeBtn.Size = UDim2.new(0.08, 0, 1, 0)
infTimeBtn.Position = UDim2.new(0.26, 0, 0, 0)
infTimeBtn.Text = "∞"
infTimeBtn.TextColor3 = colors.text
infTimeBtn.BackgroundColor3 = colors.bg2
infTimeBtn.TextSize = 14
infTimeBtn.Font = Enum.Font.GothamBold
createCorner(infTimeBtn, 5)
createStroke(infTimeBtn, colors.textDim, 0.3)

local copyLabel = Instance.new("TextLabel", settingsRow)
copyLabel.Size = UDim2.new(0.12, 0, 1, 0)
copyLabel.Position = UDim2.new(0.38, 0, 0, 0)
copyLabel.Text = "📋"
copyLabel.TextColor3 = colors.textDim
copyLabel.BackgroundTransparency = 1
copyLabel.TextSize = 12
copyLabel.TextXAlignment = Enum.TextXAlignment.Right

local copyBox = Instance.new("TextBox", settingsRow)
copyBox.Size = UDim2.new(0.1, 0, 1, 0)
copyBox.Position = UDim2.new(0.52, 0, 0, 0)
copyBox.Text = "3"
copyBox.TextColor3 = colors.text
copyBox.BackgroundColor3 = colors.bg2
copyBox.TextSize = 11
copyBox.Font = Enum.Font.GothamBold
createCorner(copyBox, 5)
createStroke(copyBox, colors.textDim, 0.3)

local infCopyBtn = Instance.new("TextButton", settingsRow)
infCopyBtn.Size = UDim2.new(0.08, 0, 1, 0)
infCopyBtn.Position = UDim2.new(0.64, 0, 0, 0)
infCopyBtn.Text = "∞"
infCopyBtn.TextColor3 = colors.text
infCopyBtn.BackgroundColor3 = colors.bg2
infCopyBtn.TextSize = 14
infCopyBtn.Font = Enum.Font.GothamBold
createCorner(infCopyBtn, 5)
createStroke(infCopyBtn, colors.textDim, 0.3)

local speedLabel = Instance.new("TextLabel", settingsRow)
speedLabel.Size = UDim2.new(0.12, 0, 1, 0)
speedLabel.Position = UDim2.new(0.76, 0, 0, 0)
speedLabel.Text = "⚡"
speedLabel.TextColor3 = colors.textDim
speedLabel.BackgroundTransparency = 1
speedLabel.TextSize = 12
speedLabel.TextXAlignment = Enum.TextXAlignment.Right

local speedBox = Instance.new("TextBox", settingsRow)
speedBox.Size = UDim2.new(0.1, 0, 1, 0)
speedBox.Position = UDim2.new(0.88, 0, 0, 0)
speedBox.Text = "0.01"
speedBox.TextColor3 = colors.text
speedBox.BackgroundColor3 = colors.bg2
speedBox.TextSize = 11
speedBox.Font = Enum.Font.GothamBold
createCorner(speedBox, 5)
createStroke(speedBox, colors.textDim, 0.3)

local statsRow = Instance.new("Frame", spamContainer)
statsRow.Size = UDim2.new(1, 0, 0, 20)
statsRow.Position = UDim2.new(0, 0, 0, 108)
statsRow.BackgroundTransparency = 1

local cmdCount = Instance.new("TextLabel", statsRow)
cmdCount.Size = UDim2.new(0.33, 0, 1, 0)
cmdCount.Text = "📝 0"
cmdCount.TextColor3 = colors.textDim
cmdCount.BackgroundTransparency = 1
cmdCount.TextSize = 10
cmdCount.Font = Enum.Font.Gotham
cmdCount.TextXAlignment = Enum.TextXAlignment.Left

local sentCount = Instance.new("TextLabel", statsRow)
sentCount.Size = UDim2.new(0.34, 0, 1, 0)
sentCount.Position = UDim2.new(0.33, 0, 0, 0)
sentCount.Text = "📤 0"
sentCount.TextColor3 = colors.textDim
sentCount.BackgroundTransparency = 1
sentCount.TextSize = 10
sentCount.Font = Enum.Font.Gotham

local spamStatus = Instance.new("TextLabel", statsRow)
spamStatus.Size = UDim2.new(0.33, 0, 1, 0)
spamStatus.Position = UDim2.new(0.67, 0, 0, 0)
spamStatus.Text = "⏸️"
spamStatus.TextColor3 = colors.textDim
spamStatus.BackgroundTransparency = 1
spamStatus.TextSize = 10
spamStatus.Font = Enum.Font.Gotham
spamStatus.TextXAlignment = Enum.TextXAlignment.Right

local controlRow = Instance.new("Frame", spamContainer)
controlRow.Size = UDim2.new(1, 0, 0, 40)
controlRow.Position = UDim2.new(0, 0, 0, 135)
controlRow.BackgroundTransparency = 1

local startBtn = Instance.new("TextButton", controlRow)
startBtn.Size = UDim2.new(0.47, -5, 1, 0)
startBtn.Position = UDim2.new(0, 0, 0, 0)
startBtn.Text = "▶ تشغيل"
startBtn.BackgroundColor3 = colors.success
startBtn.TextColor3 = colors.text
startBtn.Font = Enum.Font.GothamBold
startBtn.TextSize = 13
createCorner(startBtn, 8)
createStroke(startBtn, colors.success, 1.5)

local stopBtn = Instance.new("TextButton", controlRow)
stopBtn.Size = UDim2.new(0.47, -5, 1, 0)
stopBtn.Position = UDim2.new(0.53, 5, 0, 0)
stopBtn.Text = "⏹ إيقاف"
stopBtn.BackgroundColor3 = colors.danger
stopBtn.TextColor3 = colors.text
stopBtn.Font = Enum.Font.GothamBold
stopBtn.TextSize = 13
createCorner(stopBtn, 8)
createStroke(stopBtn, colors.danger, 1.5)

local timerDisplay = Instance.new("TextLabel", spamContainer)
timerDisplay.Size = UDim2.new(1, 0, 0, 18)
timerDisplay.Position = UDim2.new(0, 0, 0, 182)
timerDisplay.Text = "⏱️ --"
timerDisplay.TextColor3 = colors.warning
timerDisplay.BackgroundTransparency = 1
timerDisplay.TextSize = 11
timerDisplay.Font = Enum.Font.GothamBold

---------------------------------------
-- حاوية الأوامر المخصصة
---------------------------------------
local commandsContainer = Instance.new("Frame", mainFrame)
commandsContainer.Size = UDim2.new(1, -30, 1, -145)
commandsContainer.Position = UDim2.new(0, 15, 0, 135)
commandsContainer.BackgroundTransparency = 1
commandsContainer.Visible = false

local cmdsTitle = Instance.new("TextLabel", commandsContainer)
cmdsTitle.Size = UDim2.new(1, 0, 0, 25)
cmdsTitle.Text = "📝 الأوامر المخصصة"
cmdsTitle.TextColor3 = colors.primary
cmdsTitle.BackgroundTransparency = 1
cmdsTitle.Font = Enum.Font.GothamBold
cmdsTitle.TextSize = 14
cmdsTitle.TextXAlignment = Enum.TextXAlignment.Left

local addFrame = Instance.new("Frame", commandsContainer)
addFrame.Size = UDim2.new(1, 0, 0, 48)
addFrame.Position = UDim2.new(0, 0, 0, 30)
addFrame.BackgroundColor3 = colors.bg2
createCorner(addFrame, 8)
createStroke(addFrame, colors.textDim, 0.3)

local nameBox = Instance.new("TextBox", addFrame)
nameBox.Size = UDim2.new(0.35, -5, 1, 0)
nameBox.Position = UDim2.new(0, 5, 0, 0)
nameBox.PlaceholderText = "الاسم"
nameBox.Text = ""
nameBox.TextColor3 = colors.text
nameBox.TextSize = 11
nameBox.BackgroundColor3 = colors.bg3
nameBox.Font = Enum.Font.Gotham
createCorner(nameBox, 5)

local cmdBox = Instance.new("TextBox", addFrame)
cmdBox.Size = UDim2.new(0.40, -5, 1, 0)
cmdBox.Position = UDim2.new(0.37, 0, 0, 0)
cmdBox.PlaceholderText = "الأمر"
cmdBox.Text = ""
cmdBox.TextColor3 = colors.text
cmdBox.TextSize = 11
cmdBox.BackgroundColor3 = colors.bg3
cmdBox.Font = Enum.Font.Gotham
createCorner(cmdBox, 5)

local addCmdBtn = Instance.new("TextButton", addFrame)
addCmdBtn.Size = UDim2.new(0.20, -5, 1, 0)
addCmdBtn.Position = UDim2.new(0.79, 0, 0, 0)
addCmdBtn.Text = "+ إضافة"
addCmdBtn.BackgroundColor3 = colors.success
addCmdBtn.TextColor3 = colors.text
addCmdBtn.Font = Enum.Font.GothamBold
addCmdBtn.TextSize = 11
createCorner(addCmdBtn, 5)

local cmdsList = Instance.new("ScrollingFrame", commandsContainer)
cmdsList.Size = UDim2.new(1, 0, 1, -95)
cmdsList.Position = UDim2.new(0, 0, 0, 88)
cmdsList.BackgroundTransparency = 1
cmdsList.ScrollBarThickness = 3
cmdsList.ScrollBarImageColor3 = colors.primary
cmdsList.CanvasSize = UDim2.new(0, 0, 0, 0)

local function addCustomCommand(name, command)
    if #customCommands >= 8 then
        warningNotify("حد أقصى 8 أوامر مخصصة")
        return false
    end
    table.insert(customCommands, {name = name, command = command})
    successNotify("تم إضافة الأمر: " .. name)
    saveSettings()
    return true
end

local function removeCustomCommand(index)
    local cmdName = customCommands[index].name
    table.remove(customCommands, index)
    successNotify("تم حذف الأمر: " .. cmdName)
    saveSettings()
    return true
end

local function refreshCommandsList()
    for _, child in pairs(cmdsList:GetChildren()) do
        if child:IsA("Frame") then
            child:Destroy()
        end
    end
    
    cmdsList.CanvasSize = UDim2.new(0, 0, 0, #customCommands * 42 + 10)
    
    for i, cmd in ipairs(customCommands) do
        local frame = Instance.new("Frame", cmdsList)
        frame.Size = UDim2.new(1, 0, 0, 37)
        frame.Position = UDim2.new(0, 0, 0, (i-1) * 42 + 5)
        frame.BackgroundColor3 = colors.bg2
        createCorner(frame, 6)
        createStroke(frame, colors.textDim, 0.3)
        
        local nameLabel = Instance.new("TextLabel", frame)
        nameLabel.Size = UDim2.new(0.25, -5, 1, 0)
        nameLabel.Position = UDim2.new(0, 8, 0, 0)
        nameLabel.Text = cmd.name
        nameLabel.TextColor3 = colors.primary
        nameLabel.BackgroundTransparency = 1
        nameLabel.Font = Enum.Font.GothamBold
        nameLabel.TextSize = 11
        nameLabel.TextXAlignment = Enum.TextXAlignment.Left
        
        local cmdLabel = Instance.new("TextLabel", frame)
        cmdLabel.Size = UDim2.new(0.45, -5, 1, 0)
        cmdLabel.Position = UDim2.new(0.27, 0, 0, 0)
        cmdLabel.Text = cmd.command
        cmdLabel.TextColor3 = colors.text
        cmdLabel.BackgroundTransparency = 1
        cmdLabel.Font = Enum.Font.Gotham
        cmdLabel.TextSize = 11
        cmdLabel.TextXAlignment = Enum.TextXAlignment.Left
        
        local useBtn = Instance.new("TextButton", frame)
        useBtn.Size = UDim2.new(0.15, -5, 0.7, 0)
        useBtn.Position = UDim2.new(0.74, 0, 0.15, 0)
        useBtn.Text = "▶ إرسال"
        useBtn.BackgroundColor3 = colors.success
        useBtn.TextColor3 = colors.text
        useBtn.Font = Enum.Font.GothamBold
        useBtn.TextSize = 9
        createCorner(useBtn, 5)
        
        setupOneClick(useBtn, function()
            task.spawn(function()
                pcall(function()
                    ReplicatedStorage.HDAdminHDClient.Signals.CreateLog:FireServer({Message = cmd.command, Player = player, LogType = "chatLogs"})
                end)
                pcall(function()
                    ReplicatedStorage.HDAdminHDClient.Signals.RequestCommandModification:InvokeServer(cmd.command)
                end)
            end)
            successNotify("تم إرسال: " .. cmd.name)
        end)
        
        local delBtn = Instance.new("TextButton", frame)
        delBtn.Size = UDim2.new(0.08, -3, 0.7, 0)
        delBtn.Position = UDim2.new(0.91, 0, 0.15, 0)
        delBtn.Text = "✕"
        delBtn.BackgroundColor3 = colors.danger
        delBtn.TextColor3 = colors.text
        delBtn.Font = Enum.Font.GothamBold
        delBtn.TextSize = 9
        createCorner(delBtn, 5)
        
        setupOneClick(delBtn, function()
            removeCustomCommand(i)
            refreshCommandsList()
        end)
    end
end

setupOneClick(addCmdBtn, function()
    if nameBox.Text ~= "" and cmdBox.Text ~= "" then
        if addCustomCommand(nameBox.Text, cmdBox.Text) then
            nameBox.Text = ""
            cmdBox.Text = ""
            refreshCommandsList()
        end
    else
        errorNotify("املأ الاسم والأمر")
    end
end)

refreshCommandsList()

---------------------------------------
-- نافذة الإعدادات
---------------------------------------
local settingsFrame = Instance.new("Frame", mainFrame)
settingsFrame.Size = UDim2.new(1, -30, 1, -145)
settingsFrame.Position = UDim2.new(0, 15, 0, 135)
settingsFrame.BackgroundColor3 = colors.bg2
settingsFrame.BackgroundTransparency = 0.5
settingsFrame.Visible = false
createCorner(settingsFrame, 12)
createStroke(settingsFrame, colors.gold, 1)

local settingsHeader = Instance.new("Frame", settingsFrame)
settingsHeader.Size = UDim2.new(1, 0, 0, 30)
settingsHeader.BackgroundTransparency = 1

local settingsTitle = Instance.new("TextLabel", settingsHeader)
settingsTitle.Size = UDim2.new(1, -40, 1, 0)
settingsTitle.Position = UDim2.new(0, 10, 0, 0)
settingsTitle.Text = "⚙️ الإعدادات"
settingsTitle.TextColor3 = colors.gold
settingsTitle.BackgroundTransparency = 1
settingsTitle.Font = Enum.Font.GothamBold
settingsTitle.TextSize = 14
settingsTitle.TextXAlignment = Enum.TextXAlignment.Left

local scrollContainer = Instance.new("ScrollingFrame", settingsFrame)
scrollContainer.Size = UDim2.new(1, -10, 1, -40)
scrollContainer.Position = UDim2.new(0, 5, 0, 35)
scrollContainer.BackgroundTransparency = 1
scrollContainer.ScrollBarThickness = 3
scrollContainer.ScrollBarImageColor3 = colors.primary
scrollContainer.CanvasSize = UDim2.new(0, 0, 0, 750)

-- قسم الثيمات
local themeTitle = Instance.new("TextLabel", scrollContainer)
themeTitle.Size = UDim2.new(1, 0, 0, 25)
themeTitle.Text = "🎨 اختيار الثيم"
themeTitle.TextColor3 = colors.primary
themeTitle.BackgroundTransparency = 1
themeTitle.Font = Enum.Font.GothamBold
themeTitle.TextSize = 13

local allThemes = {"neon", "dark", "light", "red", "green", "purple", "ocean", "sunset", "cyber", "royal"}
local themeNames = {
    neon = "✨ نيون",
    dark = "🌙 داكن",
    light = "☀️ فاتح",
    red = "❤️ أحمر",
    green = "💚 أخضر",
    purple = "💜 بنفسجي",
    ocean = "🌊 محيط",
    sunset = "🌅 غروب",
    cyber = "💠 سايبر",
    royal = "👑 ملكي"
}

for i, theme in ipairs(allThemes) do
    local btn = Instance.new("TextButton", scrollContainer)
    btn.Size = UDim2.new(0.45, -5, 0, 28)
    btn.Position = UDim2.new((i % 2 == 1) and 0 or 0.55, 0, 0, 30 + math.floor((i-1)/2) * 35)
    btn.Text = themeNames[theme] or theme
    btn.BackgroundColor3 = getThemeColors(theme).bg2
    btn.TextColor3 = getThemeColors(theme).text
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 11
    createCorner(btn, 6)
    createStroke(btn, getThemeColors(theme).primary, 0.5)
    
    setupOneClick(btn, function()
        applyTheme(theme)
        successNotify("تم تغيير الثيم إلى: " .. themeNames[theme])
        saveSettings()
    end)
end

-- قسم الحماية
local protectTitle = Instance.new("TextLabel", scrollContainer)
protectTitle.Size = UDim2.new(1, 0, 0, 25)
protectTitle.Position = UDim2.new(0, 0, 0, 240)
protectTitle.Text = "🛡️ الحماية"
protectTitle.TextColor3 = colors.primary
protectTitle.BackgroundTransparency = 1
protectTitle.Font = Enum.Font.GothamBold
protectTitle.TextSize = 13

local function createToggle(name, pos, icon, callback)
    local frame = Instance.new("Frame", scrollContainer)
    frame.Size = UDim2.new(1, 0, 0, 38)
    frame.Position = UDim2.new(0, 0, 0, pos)
    frame.BackgroundColor3 = colors.bg2
    createCorner(frame, 6)
    createStroke(frame, colors.textDim, 0.3)
    frame.Selectable = false
    
    local iconLabel = Instance.new("TextLabel", frame)
    iconLabel.Size = UDim2.new(0, 28, 1, 0)
    iconLabel.Position = UDim2.new(0, 6, 0, 0)
    iconLabel.Text = icon or "◆"
    iconLabel.TextColor3 = colors.primary
    iconLabel.BackgroundTransparency = 1
    iconLabel.TextSize = 14
    
    local label = Instance.new("TextLabel", frame)
    label.Size = UDim2.new(0.5, -35, 1, 0)
    label.Position = UDim2.new(0, 38, 0, 0)
    label.Text = name
    label.TextColor3 = colors.text
    label.BackgroundTransparency = 1
    label.Font = Enum.Font.GothamBold
    label.TextSize = 12
    label.TextXAlignment = Enum.TextXAlignment.Left
    
    local toggle = Instance.new("TextButton", frame)
    toggle.Size = UDim2.new(0, 45, 0, 24)
    toggle.Position = UDim2.new(1, -52, 0.5, -12)
    toggle.BackgroundColor3 = colors.danger
    toggle.Text = "OFF"
    toggle.TextColor3 = colors.text
    toggle.Font = Enum.Font.GothamBold
    toggle.TextSize = 10
    createCorner(toggle, 12)
    createStroke(toggle, colors.danger, 1)
    toggle.AutoButtonColor = true
    
    local state = false
    
    local function toggleState()
        state = not state
        toggle.BackgroundColor3 = state and colors.success or colors.danger
        toggle.Text = state and "ON" or "OFF"
        toggle.UIStroke.Color = state and colors.success or colors.danger
        callback(state)
    end
    
    setupOneClick(toggle, toggleState)
    
    return toggle
end

-- 1. مضاد الفلينج
createToggle("🌀 مضاد الفلينج", 275, "🌀", function(s) 
    Settings.AntiFling = s
    
    if s then
        ProtectionThreads.AntiFling = RunService.Heartbeat:Connect(function()
            if not Settings.AntiFling then return end
            
            local char = player.Character
            if char and char:FindFirstChild("HumanoidRootPart") then
                local hrp = char.HumanoidRootPart
                if hrp.Velocity.Magnitude > 150 then
                    hrp.Velocity = Vector3.new(0, 0, 0)
                    hrp.RotVelocity = Vector3.new(0, 0, 0)
                    if lastSafePos then 
                        hrp.CFrame = lastSafePos 
                    end
                else
                    lastSafePos = hrp.CFrame
                end
            end
            
            pcall(function()
                if char then
                    local hum = char:FindFirstChildOfClass("Humanoid")
                    if hum then
                        hum.Sit = false
                        hum.PlatformStand = false
                    end
                    for _, p in pairs(char:GetChildren()) do
                        if p:IsA("BasePart") and p.Anchored then 
                            p.Anchored = false 
                        end
                        if p:IsA("BodyMover") or p:IsA("BodyVelocity") or p:IsA("BodyGyro") then
                            p:Destroy()
                        end
                    end
                end
            end)
        end)
        successNotify("مضاد الفلينج مفعل")
    else
        if ProtectionThreads.AntiFling then
            ProtectionThreads.AntiFling:Disconnect()
            ProtectionThreads.AntiFling = nil
        end
        infoNotify("مضاد الفلينج معطل")
    end
    saveSettings()
end)

-- 2. مضاد اللاق
createToggle("📋 مضاد اللاق (Logs)", 317, "📋", function(s) 
    Settings.AntiLogs = s
    
    if s then
        ProtectionThreads.AntiLogs = task.spawn(function()
            while Settings.AntiLogs do
                pcall(function()
                    local signals = ReplicatedStorage:FindFirstChild("HDAdminHDClient")
                    if signals then signals = signals:FindFirstChild("Signals") end
                    if signals then
                        for _, remote in pairs(signals:GetDescendants()) do
                            if remote:IsA("RemoteEvent") or remote:IsA("RemoteFunction") then
                                if string.find(remote.Name:lower(), "log") then
                                    remote:Destroy()
                                end
                            end
                        end
                    end
                end)
                task.wait(0.5)
            end
        end)
        successNotify("مضاد Logs مفعل")
    else
        if ProtectionThreads.AntiLogs then
            task.cancel(ProtectionThreads.AntiLogs)
            ProtectionThreads.AntiLogs = nil
        end
        infoNotify("مضاد Logs معطل")
    end
    saveSettings()
end)

-- 3. مضاد NV
createToggle("👁️ مضاد NV", 359, "👁️", function(s) 
    Settings.AntiNV = s
    
    if s then
        ProtectionThreads.AntiNV = task.spawn(function()
            while Settings.AntiNV do
                pcall(function()
                    local cam = workspace.CurrentCamera
                    if cam then
                        cam.CameraType = Enum.CameraType.Custom
                        cam.FieldOfView = 70
                    end
                end)
                task.wait(0.5)
            end
        end)
        successNotify("مضاد NV مفعل")
    else
        if ProtectionThreads.AntiNV then
            task.cancel(ProtectionThreads.AntiNV)
            ProtectionThreads.AntiNV = nil
        end
        infoNotify("مضاد NV معطل")
    end
    saveSettings()
end)

-- 4. مضاد Clogs
createToggle("🚫 مضاد Clogs", 401, "🚫", function(s) 
    Settings.AntiClogs = s
    
    if s then
        ProtectionThreads.AntiClogs = task.spawn(function()
            while Settings.AntiClogs do
                pcall(function()
                    local signals = ReplicatedStorage:FindFirstChild("HDAdminHDClient")
                    if signals then signals = signals:FindFirstChild("Signals") end
                    if signals then
                        for _, remote in pairs(signals:GetDescendants()) do
                            if remote:IsA("RemoteEvent") or remote:IsA("RemoteFunction") then
                                if string.find(remote.Name:lower(), "log") or string.find(remote.Name:lower(), "clear") then
                                    remote:Destroy()
                                end
                            end
                        end
                    end
                end)
                task.wait(0.5)
            end
        end)
        successNotify("مضاد Clogs مفعل")
    else
        if ProtectionThreads.AntiClogs then
            task.cancel(ProtectionThreads.AntiClogs)
            ProtectionThreads.AntiClogs = nil
        end
        infoNotify("مضاد Clogs معطل")
    end
    saveSettings()
end)

-- 5. مضاد CmdBar
createToggle("⌨️ مضاد CmdBar", 443, "⌨️", function(s) 
    Settings.AntiCmdBar = s
    
    if s then
        for _, g in pairs(player.PlayerGui:GetChildren()) do
            if g:IsA("ScreenGui") and (g.Name:find("Cmd") or g.Name:find("Command") or g.Name:find("Bar")) then
                g.Enabled = false
            end
        end
        
        ProtectionThreads.AntiCmdBar = task.spawn(function()
            while Settings.AntiCmdBar do
                pcall(function()
                    for _, g in pairs(player.PlayerGui:GetChildren()) do
                        if g:IsA("ScreenGui") and (g.Name:find("Cmd") or g.Name:find("Command") or g.Name:find("Bar")) then
                            g.Enabled = false
                        end
                    end
                end)
                task.wait(0.5)
            end
        end)
        successNotify("مضاد CmdBar مفعل")
    else
        if ProtectionThreads.AntiCmdBar then
            task.cancel(ProtectionThreads.AntiCmdBar)
            ProtectionThreads.AntiCmdBar = nil
        end
        for _, g in pairs(player.PlayerGui:GetChildren()) do
            if g:IsA("ScreenGui") and (g.Name:find("Cmd") or g.Name:find("Command") or g.Name:find("Bar")) then
                g.Enabled = true
            end
        end
        infoNotify("مضاد CmdBar معطل")
    end
    saveSettings()
end)

-- 6. مضاد الإشعارات القوي
createToggle("🔕 مضاد الإشعارات", 485, "🔕", function(s) 
    Settings.AntiAlerts = s
    
    if s then
        local function hideAllAlerts()
            pcall(function()
                for _, g in pairs(player.PlayerGui:GetChildren()) do
                    if g:IsA("ScreenGui") then
                        local alertNames = {"HDAdminGui", "MessageGui", "Alert", "Notification", "Notify", "System", "Warning", "Error", "Ban", "Kick"}
                        for _, name in pairs(alertNames) do
                            if string.find(g.Name, name) then
                                g.Enabled = false
                                g:Destroy()
                            end
                        end
                    end
                end
            end)
        end
        
        ProtectionThreads.AntiAlerts = task.spawn(function()
            while Settings.AntiAlerts do
                hideAllAlerts()
                task.wait(0.05)
            end
        end)
        
        local function blockNewAlerts(obj)
            if obj:IsA("ScreenGui") then
                local alertNames = {"HDAdminGui", "MessageGui", "Alert", "Notification", "Notify", "System", "Warning", "Error", "Ban", "Kick"}
                for _, name in pairs(alertNames) do
                    if string.find(obj.Name, name) then
                        obj.Enabled = false
                        obj:Destroy()
                    end
                end
            end
        end
        
        player.PlayerGui.ChildAdded:Connect(function(child)
            if Settings.AntiAlerts then
                task.wait(0.01)
                blockNewAlerts(child)
            end
        end)
        
        successNotify("مضاد الإشعارات مفعل نهائياً")
    else
        if ProtectionThreads.AntiAlerts then
            task.cancel(ProtectionThreads.AntiAlerts)
            ProtectionThreads.AntiAlerts = nil
        end
        infoNotify("مضاد الإشعارات معطل")
    end
    saveSettings()
end)

-- 7. مضاد الطرد
createToggle("🛡️ مضاد الطرد", 527, "🛡️", function(s) 
    Settings.AntiKick = s
    
    if s then
        ProtectionThreads.AntiKick = task.spawn(function()
            while Settings.AntiKick do
                pcall(function()
                    if player:FindFirstChild("Kick") then
                        player.Kick = function() end
                    end
                end)
                task.wait(1)
            end
        end)
        successNotify("مضاد الطرد مفعل")
    else
        if ProtectionThreads.AntiKick then
            task.cancel(ProtectionThreads.AntiKick)
            ProtectionThreads.AntiKick = nil
        end
        infoNotify("مضاد الطرد معطل")
    end
    saveSettings()
end)

-- 8. مضاد AFK
createToggle("💤 مضاد AFK", 569, "💤", function(s) 
    Settings.AntiAFK = s
    
    if s then
        ProtectionThreads.AntiAFK = task.spawn(function()
            while Settings.AntiAFK do
                pcall(function()
                    local char = player.Character
                    if char and char:FindFirstChild("HumanoidRootPart") then
                        local hrp = char.HumanoidRootPart
                        hrp.CFrame = hrp.CFrame + Vector3.new(0, 0, 0.01)
                        task.wait(0.1)
                        hrp.CFrame = hrp.CFrame - Vector3.new(0, 0, 0.01)
                    end
                end)
                task.wait(30)
            end
        end)
        successNotify("مضاد AFK مفعل")
    else
        if ProtectionThreads.AntiAFK then
            task.cancel(ProtectionThreads.AntiAFK)
            ProtectionThreads.AntiAFK = nil
        end
        infoNotify("مضاد AFK معطل")
    end
    saveSettings()
end)

-- 💾 أزرار الحفظ والتحميل في الإعدادات
local saveLoadTitle = Instance.new("TextLabel", scrollContainer)
saveLoadTitle.Size = UDim2.new(1, 0, 0, 25)
saveLoadTitle.Position = UDim2.new(0, 0, 0, 620)
saveLoadTitle.Text = "💾 الحفظ والتحميل"
saveLoadTitle.TextColor3 = colors.gold
saveLoadTitle.BackgroundTransparency = 1
saveLoadTitle.Font = Enum.Font.GothamBold
saveLoadTitle.TextSize = 13

local saveBtn = Instance.new("TextButton", scrollContainer)
saveBtn.Size = UDim2.new(0.45, -5, 0, 35)
saveBtn.Position = UDim2.new(0, 0, 0, 655)
saveBtn.Text = "💾 حفظ الإعدادات"
saveBtn.BackgroundColor3 = colors.success
saveBtn.TextColor3 = colors.text
saveBtn.Font = Enum.Font.GothamBold
saveBtn.TextSize = 12
createCorner(saveBtn, 8)
createStroke(saveBtn, colors.success, 1.5)

setupOneClick(saveBtn, function()
    saveSettings()
end)

local loadBtn = Instance.new("TextButton", scrollContainer)
loadBtn.Size = UDim2.new(0.45, -5, 0, 35)
loadBtn.Position = UDim2.new(0.55, 5, 0, 655)
loadBtn.Text = "📂 تحميل الإعدادات"
loadBtn.BackgroundColor3 = colors.info
loadBtn.TextColor3 = colors.text
loadBtn.Font = Enum.Font.GothamBold
loadBtn.TextSize = 12
createCorner(loadBtn, 8)
createStroke(loadBtn, colors.info, 1.5)

setupOneClick(loadBtn, function()
    loadSettings()
    refreshCommandsList()
end)

---------------------------------------
-- نافذة المعلومات
---------------------------------------
local infoFrame = Instance.new("Frame", mainFrame)
infoFrame.Size = UDim2.new(1, -30, 1, -145)
infoFrame.Position = UDim2.new(0, 15, 0, 135)
infoFrame.BackgroundColor3 = colors.bg2
infoFrame.BackgroundTransparency = 0.5
infoFrame.Visible = false
createCorner(infoFrame, 12)
createStroke(infoFrame, colors.info, 1.5)

local infoHeader = Instance.new("Frame", infoFrame)
infoHeader.Size = UDim2.new(1, 0, 0, 30)
infoHeader.BackgroundTransparency = 1

local infoTitle = Instance.new("TextLabel", infoHeader)
infoTitle.Size = UDim2.new(1, -40, 1, 0)
infoTitle.Position = UDim2.new(0, 10, 0, 0)
infoTitle.Text = "ℹ️ المعلومات"
infoTitle.TextColor3 = colors.info
infoTitle.BackgroundTransparency = 1
infoTitle.Font = Enum.Font.GothamBold
infoTitle.TextSize = 14
infoTitle.TextXAlignment = Enum.TextXAlignment.Left

local infoScroll = Instance.new("ScrollingFrame", infoFrame)
infoScroll.Size = UDim2.new(1, -10, 1, -40)
infoScroll.Position = UDim2.new(0, 5, 0, 35)
infoScroll.BackgroundTransparency = 1
infoScroll.ScrollBarThickness = 3
infoScroll.ScrollBarImageColor3 = colors.info
infoScroll.CanvasSize = UDim2.new(0, 0, 0, 700)

local function createInfoSection(title, icon, pos)
    local frame = Instance.new("Frame", infoScroll)
    frame.Size = UDim2.new(1, 0, 0, 28)
    frame.Position = UDim2.new(0, 0, 0, pos)
    frame.BackgroundTransparency = 1
    
    local label = Instance.new("TextLabel", frame)
    label.Size = UDim2.new(1, 0, 1, 0)
    label.Text = icon .. " " .. title
    label.TextColor3 = colors.info
    label.BackgroundTransparency = 1
    label.Font = Enum.Font.GothamBold
    label.TextSize = 13
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.TextYAlignment = Enum.TextYAlignment.Bottom
    
    return frame
end

local function createInfoText(text, pos, color, size)
    local label = Instance.new("TextLabel", infoScroll)
    label.Size = UDim2.new(1, -10, 0, 18)
    label.Position = UDim2.new(0, 10, 0, pos)
    label.Text = text
    label.TextColor3 = color or colors.textDim
    label.BackgroundTransparency = 1
    label.Font = Enum.Font.Gotham
    label.TextSize = size or 11
    label.TextXAlignment = Enum.TextXAlignment.Left
    return label
end

local pos = 5

createInfoSection("المعلومات الأساسية", "📋", pos)
pos = pos + 33
createInfoText("👨‍💻 المطور: mo7sma0550", pos, colors.info, 12)
pos = pos + 22
createInfoText("📌 الإصدار: V24 (Fully Fixed)", pos)
pos = pos + 22
createInfoText("📅 التاريخ: 14/07/2026", pos)
pos = pos + 22
createInfoText("⚡ الحالة: نشط ومُحسَّن", pos)
pos = pos + 35

createInfoSection("المميزات الجديدة", "✨", pos)
pos = pos + 33
createInfoText("🔔 نظام إشعارات احترافي", pos, colors.gold)
pos = pos + 22
createInfoText("💾 حفظ تلقائي للإعدادات", pos, colors.gold)
pos = pos + 22
createInfoText("📂 تحميل الإعدادات بضغطة", pos, colors.gold)
pos = pos + 35

createInfoSection("إحصائيات المستخدم", "📊", pos)
pos = pos + 33

local statsText = Instance.new("TextLabel", infoScroll)
statsText.Size = UDim2.new(1, -10, 0, 18)
statsText.Position = UDim2.new(0, 10, 0, pos)
statsText.Text = "📝 الأوامر المرسلة: 0"
statsText.TextColor3 = colors.textDim
statsText.BackgroundTransparency = 1
statsText.Font = Enum.Font.Gotham
statsText.TextSize = 11
statsText.TextXAlignment = Enum.TextXAlignment.Left
pos = pos + 22

local protectStats = Instance.new("TextLabel", infoScroll)
protectStats.Size = UDim2.new(1, -10, 0, 18)
protectStats.Position = UDim2.new(0, 10, 0, pos)
protectStats.Text = "🛡️ الحمايات المفعلة: 0/8"
protectStats.TextColor3 = colors.textDim
protectStats.BackgroundTransparency = 1
protectStats.Font = Enum.Font.Gotham
protectStats.TextSize = 11
protectStats.TextXAlignment = Enum.TextXAlignment.Left
pos = pos + 22

local timeStats = Instance.new("TextLabel", infoScroll)
timeStats.Size = UDim2.new(1, -10, 0, 18)
timeStats.Position = UDim2.new(0, 10, 0, pos)
timeStats.Text = "⏱️ وقت التشغيل: 00:00"
timeStats.TextColor3 = colors.textDim
timeStats.BackgroundTransparency = 1
timeStats.Font = Enum.Font.Gotham
timeStats.TextSize = 11
timeStats.TextXAlignment = Enum.TextXAlignment.Left
pos = pos + 22

local cmdCountStats = Instance.new("TextLabel", infoScroll)
cmdCountStats.Size = UDim2.new(1, -10, 0, 18)
cmdCountStats.Position = UDim2.new(0, 10, 0, pos)
cmdCountStats.Text = "📂 الأوامر المخصصة: 0"
cmdCountStats.TextColor3 = colors.textDim
cmdCountStats.BackgroundTransparency = 1
cmdCountStats.Font = Enum.Font.Gotham
cmdCountStats.TextSize = 11
cmdCountStats.TextXAlignment = Enum.TextXAlignment.Left
pos = pos + 35

createInfoSection("مميزات السكربت", "🔥", pos)
pos = pos + 33
local features = {
    "✅ 10 ثيمات احترافية",
    "✅ 8 خيارات حماية متكاملة",
    "✅ نظام إشعارات داخلي",
    "✅ حفظ وتحميل تلقائي",
    "✅ 8 أوامر مخصصة",
    "✅ سرعة تكرار فائقة",
}
for _, feature in ipairs(features) do
    createInfoText(feature, pos, colors.text, 11)
    pos = pos + 20
end
pos = pos + 15

createInfoSection("نصائح واستخدامات", "💡", pos)
pos = pos + 33
local tips = {
    "• استخدم ; للفصل بين الأوامر المتعددة",
    "• الإعدادات تُحفظ تلقائياً كل 30 ثانية",
    "• استخدم أزرار الحفظ والتحميل يدوياً",
    "• الإشعارات تظهر في أعلى الشاشة",
}
for _, tip in ipairs(tips) do
    createInfoText(tip, pos, colors.textDim, 11)
    pos = pos + 20
end

pos = pos + 20
infoScroll.CanvasSize = UDim2.new(0, 0, 0, pos + 20)

---------------------------------------
-- تحديث الإحصائيات
---------------------------------------
task.spawn(function()
    while true do
        statsText.Text = "📝 الأوامر المرسلة: " .. Spam.TotalSent
        local activeProtections = 0
        for _, value in pairs(Settings) do
            if value then activeProtections = activeProtections + 1 end
        end
        protectStats.Text = "🛡️ الحمايات المفعلة: " .. activeProtections .. "/8"
        local elapsed = os.time() - Spam.StartTime
        local minutes = math.floor(elapsed / 60)
        local seconds = elapsed % 60
        timeStats.Text = "⏱️ وقت التشغيل: " .. string.format("%02d:%02d", minutes, seconds)
        cmdCountStats.Text = "📂 الأوامر المخصصة: " .. #customCommands
        task.wait(1)
    end
end)

---------------------------------------
-- منطق السبام
---------------------------------------
local function fireSpam(cmd)
    pcall(function()
        ReplicatedStorage.HDAdminHDClient.Signals.CreateLog:FireServer({Message = cmd, Player = player, LogType = "chatLogs"})
    end)
    pcall(function()
        ReplicatedStorage.HDAdminHDClient.Signals.RequestCommandModification:InvokeServer(cmd)
    end)
end

setupOneClick(infTimeBtn, function()
    Spam.InfiniteTime = not Spam.InfiniteTime
    if Spam.InfiniteTime then
        infTimeBtn.BackgroundColor3 = colors.warning
        infTimeBtn.UIStroke.Color = colors.warning
        timeBox.Text = "∞"
        timeBox.TextColor3 = colors.warning
        infoNotify("الوقت لا نهائي")
    else
        infTimeBtn.BackgroundColor3 = colors.bg2
        infTimeBtn.UIStroke.Color = colors.textDim
        timeBox.Text = "10"
        timeBox.TextColor3 = colors.text
        infoNotify("الوقت محدود")
    end
    saveSettings()
end)

setupOneClick(infCopyBtn, function()
    Spam.InfiniteCopies = not Spam.InfiniteCopies
    if Spam.InfiniteCopies then
        infCopyBtn.BackgroundColor3 = colors.warning
        infCopyBtn.UIStroke.Color = colors.warning
        copyBox.Text = "∞"
        copyBox.TextColor3 = colors.warning
        infoNotify("النسخ لا نهائية")
    else
        infCopyBtn.BackgroundColor3 = colors.bg2
        infCopyBtn.UIStroke.Color = colors.textDim
        copyBox.Text = "3"
        copyBox.TextColor3 = colors.text
        infoNotify("النسخ محدودة")
    end
    saveSettings()
end)

inputBox:GetPropertyChangedSignal("Text"):Connect(function()
    local count = 0
    for _ in string.gmatch(inputBox.Text, ";") do count = count + 1 end
    cmdCount.Text = "📝 " .. count
end)

setupOneClick(startBtn, function()
    local cmds = {}
    for c in string.gmatch(inputBox.Text, ";[^;\n]+") do
        table.insert(cmds, c)
    end
    if #cmds == 0 then 
        errorNotify("لا توجد أوامر")
        return 
    end
    
    local duration = Spam.InfiniteTime and math.huge or (tonumber(timeBox.Text) or 10)
    local copies = Spam.InfiniteCopies and math.huge or (tonumber(copyBox.Text) or 3)
    local speed = tonumber(speedBox.Text) or 0.01
    
    Spam.Active = true
    Spam.TotalSent = 0
    spamStatus.Text = "▶️"
    spamStatus.TextColor3 = colors.success
    startBtn.BackgroundColor3 = colors.warning
    startBtn.Text = "⏳"
    successNotify("بدء التكرار...")
    
    task.spawn(function()
        local startTime = tick()
        while Spam.Active and (tick() - startTime) < duration do
            local elapsed = tick() - startTime
            if not Spam.InfiniteTime then
                local remaining = math.ceil(duration - elapsed)
                timerDisplay.Text = "⏱️ " .. remaining .. "s"
            end
            
            local copyCount = 0
            while Spam.Active and copyCount < copies do
                for _, c in pairs(cmds) do
                    if not Spam.Active then break end
                    task.spawn(function() fireSpam(c) end)
                    Spam.TotalSent = Spam.TotalSent + 1
                    sentCount.Text = "📤 " .. Spam.TotalSent
                    task.wait(0.001)
                end
                copyCount = copyCount + 1
                if copies == math.huge then
                    task.wait(speed)
                else
                    break
                end
            end
            task.wait(speed)
        end
        
        Spam.Active = false
        spamStatus.Text = "⏸️"
        spamStatus.TextColor3 = colors.textDim
        startBtn.BackgroundColor3 = colors.success
        startBtn.Text = "▶"
        successNotify("تم إيقاف التكرار - " .. Spam.TotalSent .. " أمر")
        if not Spam.InfiniteTime then
            timerDisplay.Text = "⏱️ --"
        end
        saveSettings()
    end)
end)

setupOneClick(stopBtn, function()
    Spam.Active = false
    spamStatus.Text = "⏸️"
    spamStatus.TextColor3 = colors.textDim
    startBtn.BackgroundColor3 = colors.success
    startBtn.Text = "▶"
    infoNotify("تم إيقاف التكرار")
    if not Spam.InfiniteTime then
        timerDisplay.Text = "⏱️ --"
    end
    saveSettings()
end)

---------------------------------------
-- أحداث الأزرار
---------------------------------------
setupOneClick(toggleBtn, function()
    mainFrame.Visible = not mainFrame.Visible
    if mainFrame.Visible then
        toggleBtn.Visible = false
        infoNotify("تم فتح السكربت")
    else
        toggleBtn.Visible = true
        saveOnClose()
    end
end)

setupOneClick(infoBtn, function()
    infoFrame.Visible = not infoFrame.Visible
    if infoFrame.Visible then
        infoBtn.Text = "✕"
        infoBtn.TextColor3 = colors.danger
        spamContainer.Visible = false
        commandsContainer.Visible = false
        settingsFrame.Visible = false
    else
        infoBtn.Text = "ℹ️"
        infoBtn.TextColor3 = colors.info
        spamContainer.Visible = true
    end
end)

setupOneClick(themeBtn, function()
    local currentIndex = nil
    for i, theme in ipairs(allThemes) do
        if theme == currentTheme then
            currentIndex = i
            break
        end
    end
    if not currentIndex then currentIndex = 1 end
    local nextIndex = currentIndex % #allThemes + 1
    local nextTheme = allThemes[nextIndex]
    applyTheme(nextTheme)
    successNotify("تم تغيير الثيم إلى: " .. themeNames[nextTheme])
    saveSettings()
end)

setupOneClick(settingsBtn, function()
    settingsFrame.Visible = not settingsFrame.Visible
    if settingsFrame.Visible then
        settingsBtn.Text = "✕"
        settingsBtn.TextColor3 = colors.danger
        spamContainer.Visible = false
        commandsContainer.Visible = false
        infoFrame.Visible = false
        infoNotify("فتح الإعدادات")
    else
        settingsBtn.Text = "⚙️"
        settingsBtn.TextColor3 = colors.gold
        spamContainer.Visible = true
        saveOnClose()
    end
end)

setupOneClick(closeBtn, function()
    mainFrame.Visible = false
    toggleBtn.Visible = true
    saveOnClose()
    infoNotify("تم الخروج من السكربت")
end)

setupOneClick(minimizeBtn, function()
    mainFrame.Visible = false
    toggleBtn.Visible = true
    infoNotify("تم تصغير السكربت")
end)

---------------------------------------
-- تأثيرات حركية
---------------------------------------
task.spawn(function()
    while true do
        local info = TweenInfo.new(4, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut)
        local colors = getThemeColors(currentTheme)
        
        local stroke = mainFrame:FindFirstChild("UIStroke")
        if stroke then
            TweenService:Create(stroke, info, {Color = colors.primary}):Play()
        end
        TweenService:Create(toggleBtn.UIStroke, info, {Color = colors.primary}):Play()
        
        task.wait(4)
        
        if stroke then
            TweenService:Create(stroke, info, {Color = colors.secondary}):Play()
        end
        TweenService:Create(toggleBtn.UIStroke, info, {Color = colors.secondary}):Play()
        
        task.wait(4)
    end
end)

---------------------------------------
-- 🚀 تشغيل السكربت
---------------------------------------

-- تحميل الإعدادات عند التشغيل
task.wait(0.5)
loadSettings()

-- بدء الحفظ التلقائي
autoSaveLoop()

-- إظهار الأيقونة فقط في البداية
mainFrame.Visible = false
toggleBtn.Visible = true

-- إشعار الترحيب
showNotification("🔥 MG9 ULTIMATE HUB", "تم التحميل بنجاح!\nالمطور: mo7sma0550", colors.primary, 4)

print("╔══════════════════════════════════════════════════════════╗")
print("║  🔥 MG9 ULTIMATE HUB V24 - FULLY FIXED 🔥             ║")
print("║  👨‍💻 المطور: mo7sma0550                                ║")
print("║  🔔 نظام إشعارات داخلي احترافي                       ║")
print("║  💾 حفظ تلقائي كل 30 ثانية                          ║")
print("║  📂 تحميل الإعدادات تلقائياً                        ║")
print("║  ✅ مضاد الإشعارات يخفي نهائياً                     ║")
print("║  ⚡ سرعة إرسال فائقة (0.001 ثانية)                  ║")
print("╚══════════════════════════════════════════════════════════╝")
